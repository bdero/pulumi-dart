name: pulumi_integration_tests
description: Integration tests for the Pulumi Dart SDK
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  pulumi:
    path: ../../sdk/dart
  grpc: ^5.0.0
  protobuf: ^6.0.0

dev_dependencies:
  test: ^1.25.0
  lints: ^3.0.0
