name: basic_random_test
description: Integration test project using pulumi-random provider
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  pulumi:
    path: ../../../../sdk/dart
